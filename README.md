# chromahaus
A single-file Python 3 CLI for the [abuse.ch](https://abuse.ch/) community APIs. Query reported malware URLs, hosts, hash es, families, IOCs, botnet C2s, and SSL certs _ then paint the results across a terminal that refuses to be beige.
